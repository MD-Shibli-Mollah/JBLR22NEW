    SUBROUTINE BD.DR.TO.CHRG.ACCT
    $INSERT GLOBUS.BP I_COMMON
    $INSERT GLOBUS.BP I_EQUATE
    $INSERT GLOBUS.BP I_F.DRAWINGS

    Y.APP.ACCOUNT = R.NEW(TF.DR.DRAWDOWN.ACCOUNT)
    IF Y.APP.ACCOUNT NE '' THEN
        R.NEW(TF.DR.CHARGE.ACCOUNT) = Y.APP.ACCOUNT
        R.NEW(TF.DR.PROV.COVER.ACCT) = Y.APP.ACCOUNT
    END
    RETURN
END
